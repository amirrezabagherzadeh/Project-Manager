# Claude Code Instructions

پیش از هر تحلیل، Proposal یا تغییر کد، فایل `AGENTS.md` در Root را کامل بخوان و از آن به‌عنوان حافظهٔ اصلی پروژه استفاده کن.

سپس اسناد مرتبط در `docs/` و Change فعال در `openspec/changes/` را بخوان. این فایل عمداً قوانین را تکرار نمی‌کند تا Claude Code و Codex از یک منبع مشترک پیروی کنند. در صورت تعارض، ترتیب اعتبار تعریف‌شده در `AGENTS.md` ملاک است.

برای قابلیت یا تغییر رفتاری جدید ابتدا از دستور `/opsx:propose` استفاده کن؛ برای اجرای Tasks تأییدشده از `/opsx:apply` و پس از تکمیل و Validation از `/opsx:archive` استفاده کن.

